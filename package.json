{
  "name": "reqwall",
  "version": "1.0.2",
  "description": "A lightweight rate limiting middleware for Express with token bucket and sliding window algorithms",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -p tsconfig.example.json && node dist/example/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "rate-limiter",
    "express",
    "middleware",
    "token-bucket",
    "sliding-window"
  ],
  "author": "Ifechukwu Nwokedi Emmanuel",
  "license": "MIT",
  "type": "commonjs",
   "repository": {
    "type": "git",
    "url": "https://github.com/PerryAlex-hub/reqwall-sdk"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.6.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
